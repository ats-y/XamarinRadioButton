<?xml version="1.0" encoding="utf-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="RadioButton.Views.MainPage">
    <StackLayout
        x:Name="CheckBoxArea">
        <!-- Place new controls here -->
        <Label Text="Welcome to Xamarin.Forms!"
               HorizontalOptions="CenterAndExpand"
               VerticalOptions="Start" />

        <CheckBox x:Name="Check1"
                  CheckedChanged="OnChangeCheckbox"
                  IsChecked="{Binding IsChecked1.Value}"/>

        <CheckBox x:Name="Check2"
                  CheckedChanged="OnChangeCheckbox"
                  IsChecked="{Binding IsChecked2.Value}"/>

        <CheckBox x:Name="Check3"
                  CheckedChanged="OnChangeCheckbox"
                  IsChecked="{Binding IsChecked3.Value}"/>

        <Button Text="test"
                Command="{Binding TestCommand}"/>
    </StackLayout>
</ContentPage>